name: Release Please

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: googleapis/release-please-action@v4
        id: release
        with:
          release-type: simple

#      - name: Update major tag
#        if: steps.release.outputs.release_created == 'true'
#        env:
#          VERSION: ${{ steps.release.outputs.tag_name }}
#        run: |
#          # Safely strip any leading text/prefixes to isolate the semantic version numbers
#          CLEAN_VERSION=$(echo "$VERSION" | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')
#          MAJOR=v$(echo "$CLEAN_VERSION" | cut -d. -f1)
#
#          git config user.name "github-actions[bot]"
#          git config user.email "github-actions[bot]@users.noreply.github.com"
#
#          git fetch --tags
#
#          git tag -fa "$MAJOR" -m "Update $MAJOR tag to $VERSION"
#          git push origin "$MAJOR" --force
